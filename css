---
---

@import url('https://fonts.googleapis.com/css?family=Montserrat&display=swap');

// Put your scss code here
$color-navbar: rgb(11, 12, 16);
$color-page-1: rgb(29,161,242);
$color-page-2: rgb(250, 78, 78);
$color-page-3: rgb(19,31,51);
$color-page-4: rgb(144,113,99);
$color-font: rgb(237,245,225);
$color-container-background: rgb(31,40,51);
$color-white: rgb(255, 255, 255);
$color-active-btn-background: rgb(113,113,113);
$color-background-caption: rgba(0, 0, 0, 0.507);
$color-modal-background: rgba(0, 0, 0, 0.4);
$color-modal-content-background: rgba(254,254,254, 1);
$color-close-btn: rgb(170, 170, 170);
$color-french-open: rgb(194, 59, 34);
$color-wimby: rgb(84, 0, 139);
$color-us-open: rgb(60, 99, 142);

// units
$container-width: 80%;
$container-height: 100vh;
$navbar-padding-tb: 25px;
$navbar-padding-lr: 10px;
$icon-font-size: 3em;

// apply to html and body. Make sure height is defined so children can define height
// with respect to parent as well
html, body {
    margin: 0;
    padding: 0;
    height: 100%;
    width: 100%;
}

// general navbar class for both header and footer
.nav {
    ul {
        margin: 0;
    }

    li {
        display: inline-block;
        list-style: none;
        padding: 0 10px;
    }

    a {
        color: $color-font;
        text-decoration: none;

        :hover {
            cursor: pointer;
        }
    }
}

// site container contains all of the content in the site, including header and footer
.site-container {
    display: flex;
    flex-direction: column;
    // min-height: 100vh;
    background-color: $color-container-background;
    font-family: 'Montserrat', sans-serif;

    // main header will hold the navbar
    .main-header {
        background-color: $color-navbar;
        padding: $navbar-padding-tb $navbar-padding-lr;  // decrease padding on scroll
        transition: 0.3s;  // transition effect to smoothen padding decrease
        display: flex;
        justify-content: flex-start;
        align-items: center;
        font-size: 1.2rem;
        // make the navbar sticky
        position: fixed;
        top: 0;
        width: 100%;
        // make sure navbar doesn't get overlapped
        z-index: 1;

        // specifically header navbar
        #main-nav {
            font-size: 1.1em;

            a:hover {
                cursor: pointer;
            }
        }
    }

    // The sticky class is added to the navbar with JS when it reaches its scroll position
    // found from W3Schools: https://www.w3schools.com/howto/howto_js_navbar_sticky.asp

    // container for content in the middle
    .container {
        color: $color-font;
        margin: 0 auto;
        padding: 0 1em;
        min-height: $container-height;
        width: $container-width;
        display: flex;
        flex-direction: column;
        font-size: 1em;
        text-align: center;
        align-items: center;
        justify-content: center;

        flex-grow: 1;

        h1 {
            font-weight: bold;
            font-size: 3em;
            margin-bottom: 0.25em;
        }

        p {
            width: 80%;
            font-size: 1em;
        }
    }

    // container for home section of the website
    #intro-container {
        padding-top: ($navbar-padding-tb * 3);  // in order that navbar doesn't cut into page content
        text-align: center;
        align-items: center;
        justify-content: center;
        position: relative;
        overflow: hidden;

        #intro-vid {
            height: 100%;
            width: auto;
            position: relative;
            z-index: -1;
        }

        .overlay {
            color: $color-font;
            background-color: $color-background-caption;
            width: 80%;
            position: absolute;
            margin: 0 auto;
            text-align: center;
            z-index: 0;

            .vid-credit {
                font-size: 0.8em;

                .credit-link {
                    color: $color-font;
                }

                .credit-link:hover {
                    color: $color-page-1;
                    cursor: pointer;
                }
            }

            p {
                margin: auto;
                padding-bottom: 1em;
            }
        }
    }

    #about-container {
        background-color: $color-page-2;

        h1 {
            padding-bottom: 0.5em;
        }

        .column-layout {
            column-count: 3;
            column-rule-style: solid;
            column-rule-width: 2px;
            column-rule-color: $color-french-open;

            font-size: 0.8em;

            .col {
                margin: 0;
                text-align: center;
                font-size: 1.2em;
                margin: auto 1em auto 1em;

                .fa-laptop-code, .fa-book, .fa-table-tennis {
                    font-size: $icon-font-size;
                    padding-bottom: 0.5em;
                }
            }
        }
    }

    #cs-container {
        background-color: $color-page-3;
        background-image: url(../media/cs_illinois_logo.png);
        background-size: 75% auto;
        background-position: 50% 50%;
        background-repeat: no-repeat;
        background-attachment: fixed;
        bottom: 0;
        left: 0;
        text-align: center;

        p {
            margin: auto;
            padding-bottom: 1em;
        }

        #cs-text {
            background-color: $color-background-caption;
        }
    }

    #other-container {
        background-color: $color-page-4;
        min-height: 110vh;
        justify-content: center;

        /*
        .slideshow-container {
            max-width: 80%;
            position: relative;
            margin: auto;

            // hide slides by default
            .slides {
                display: none;

                .slidenum {
                    color: $color-white;
                    font-size: 0.8em;
                    padding: 8px 12px;
                    position: absolute;
                    top: 0;
                    background-color: $color-background-caption;
                }

                // caption text
                .caption {
                    color: $color-white;
                    font-size: 1em;
                    font-weight: bold;
                    margin: 0 auto;
                    position: absolute;
                    bottom: 0;
                    left: 25%;
                    right: 15%;
                    text-align: center;
                    width: 70%;

                    .text-blurb {
                        background-color: $color-background-caption;

                        .title {
                            font-size: 1.3em;
                        }

                        .img-credit {
                            font-size: 0.7em;

                            .credit-link {
                                color: $color-font;
                            }

                            .credit-link:hover {
                                cursor: pointer;
                            }
                        }
                    }
                }

                .slide-imgs {
                    //object-fit: contain;
                    width: 100%;
                    height: 70vh;
                }

                .slide-imgs:hover {
                    cursor: pointer;
                }
            }

            // Fading animation
            .fade {
                -webkit-animation-name: fade;
                -webkit-animation-duration: 1.5s;
                animation-name: fade;
                animation-duration: 1.5s;
            }

            // CSS3 animation
            @-webkit-keyframes fade {
                from {opacity: .4}
                to {opacity: 1}
            }

            @keyframes fade {
                from {opacity: .4}
                to {opacity: 1}
            }

            #prev-btn, #next-btn {
                justify-content: space-around;
                cursor: pointer;
                position: absolute;
                top: 50%;
                width: auto;
                margin-top: -5vh;
                padding: 5vh;
                color: $color-white;
                font-weight: bold;
                font-size: 1.2em;
                transition: 0.6s ease;
                border-radius: 0 3px 3px 0;
                user-select: none;
            }

            #prev-btn {
                left: 0;
            }
            // position next button to the right
            #next-btn {
                right: 0;
            }

            #prev-btn:hover, #next-btn:hover {
                background-color: rgba(0,0,0,0.8);
            }
        }
        */
    }

    /*
    .modal {
        display: none;
        position: fixed;  // make sure modal page stays in place
        z-index: 1;       // keep page on top?
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: auto;
        background-color: $color-modal-background;

        .modal-content {
            //background-color: $color-modal-content-background;
            margin: 3% auto 0% auto;
            padding: 0;
            width: 80%;
            height: 80%;

            .close-btn {
                float: right;
                color: $color-close-btn;
                font-size: 2em;
                padding-bottom: 0.25em;
                font-weight: bold;
            }

            .close-btn:hover, .close-btn:focus {
                cursor: pointer;
            }

            #french-close-btn:hover {
                color: $color-french-open;
            }

            #wimby-close-btn:hover {
                color: $color-wimby;
            }

            #us-close-btn:hover {
                color: $color-us-open;
            }

            .modal-img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }

            .modal-caption {
                color: $color-white;
                font-size: 1.2em;
                font-weight: bold;
                margin: 0px auto 10% auto;
                position: absolute;
                bottom: 0;
                left: 25%;
                right: 25%;
                text-align: center;
                width: auto;

                .modal-text {
                    background-color: $color-background-caption;
                }

                .modal-title {
                    font-size: 1.5em;
                }
            }
        }
    }
    */

    .main-footer {
        background-color: $color-navbar;
        color: $color-white;
        padding: 0 30px;

        .main-footer-container {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            flex-shrink: 0;
        }

        #footer-nav {
            display: flex;
            align-items: center;
            justify-content: flex-start;

            li {
                padding: 0 1em;
            }

            img {
                width: 30px;
                height: 30px;
            }

            a:hover {
                cursor: pointer;
            }
        }
    }
}
